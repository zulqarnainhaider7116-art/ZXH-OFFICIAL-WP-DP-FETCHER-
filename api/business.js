export default function handler(req,res){res.setHeader("Content-Type","application/json");const url=String(req.query?.url||"").trim();if(!url)return res.status(400).json({success:false,error:"Enter a business number or public URL."});return res.status(404).json({success:false,error:"No official unauthenticated endpoint exposes arbitrary WhatsApp Business cover photos."});}