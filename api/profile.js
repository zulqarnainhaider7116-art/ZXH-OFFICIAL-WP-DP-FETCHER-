export default function handler(req,res){res.setHeader("Content-Type","application/json");const url=String(req.query?.url||"").trim();if(!url)return res.status(400).json({success:false,error:"Enter a phone number or wa.me URL."});return res.status(404).json({success:false,error:"No official unauthenticated WhatsApp endpoint exposes another user's DP by phone number. This build does not bypass privacy or fabricate images."});}